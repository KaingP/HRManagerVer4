import { Member, Shift } from "./data_loader";
import { SaleTransaction } from "../server"; // we need to export SaleTransaction or move it

export function calculateCompetitionStats(members: Member[], shifts: Shift[], sales: any[], incidents: any[]) {
    // ... we will implement this
}
